# Screenshots

Placeholder directory for UI screenshots referenced by the root `README.md`.

Drop the following PNGs here (they are referenced with placeholder links until added):

| File | View |
|------|------|
| `login.png` | Login / registration screen |
| `dashboard.png` | Dashboard with KPI cards |
| `datasets.png` | Dataset upload + statistics |
| `experiments.png` | AutoML experiment runs & metrics |
| `models.png` | Model registry + prediction |

> Tip: run the app (`make up` or the local instructions in the README), then
> capture 1280×800 screenshots in both light and dark themes.
