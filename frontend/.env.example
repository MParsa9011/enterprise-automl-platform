# Base URL for the API. In development the Vite dev server proxies /api to the
# backend, so the default relative base works out of the box.
VITE_API_BASE_URL=/api/v1

# Optional: override the dev proxy target (defaults to http://localhost:8000).
VITE_API_PROXY_TARGET=http://localhost:8000
